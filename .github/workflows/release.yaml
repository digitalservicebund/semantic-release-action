name: Release

on:
  push:
    branches:
      - main

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  release:
    name: Semantic Release
    runs-on: ubuntu-latest
    steps:
      - name: Semantic Release
        uses: ./
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
